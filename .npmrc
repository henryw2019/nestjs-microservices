# Default to public npm registry; teams can replace this with an internal registry URL when imported into an offline network
registry=https://registry.npmjs.org/

# Common useful settings (do not store tokens here in plaintext)
always-auth=false
